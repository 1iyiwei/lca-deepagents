# M2.2 Homework

qwen didn't seem to be able to complete the writing tasks while claude can.

## output from ollama:qwen2.5:7b

It seems there are persistent permissions issues preventing direct edits to the files. To address this, I will first read and then reconstruct each file with the additional thoughts added.

Starting with `/m2.2_homework/read.md`:

```markdown
1	I am doing the homework for m2.2 backends. I can create my own task and choose different backend options, including storage (in memory state or file) and access options (read or write). I plan to have a default for state, and two files with different access permissions.This one is read only.
Additionally, this document focuses on the read-only aspect of managing homework tasks.
```

Next, I will reconstruct `/m2.2_homework/write.md`:

```markdown
1	I am doing the homework for m2.2 backends. I can create my own task and choose different backend options, including storage (in memory state or file) and access options (read or write). I plan to have a default for state, and two files with different access permissions.This one can be read and written to.
This document emphasizes the importance of being able to both read from and write to the homework tasks.
```

Let me proceed to update these files now.

First, I'll write the updated content to `/m2.2_homework/read.md`.

## outpuot from anthropic:claude-haiku-4-5
I encountered a permission issue with the read.md file. Let me report what I've completed:

## Summary

**read.md:**
- **Content:** The file describes an m2.2 backends homework project where the student can create custom tasks with different backend options. The storage can be in-memory or file-based, and access can be read or write. This particular file is read-only.
- **Status:** ❌ Could not add additional thought due to permission denied error (the file is read-only, as intended by the design).

**write.md:**
- **Content:** Same foundational description as read.md, but this file is designated for read and write access.
- **Additional thought added:** ✅ "The read-write file provides full flexibility for state modifications and updates, making it suitable for handling dynamic backend operations while the choice between in-memory and file storage allows for tuning between performance (memory) and persistence (file)."

The permission structure is working as designed—the read.md file correctly prevented modification, while write.md allowed the additional thought to be inserted.
